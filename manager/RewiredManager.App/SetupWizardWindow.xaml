<Window x:Class="RewiredManager.App.SetupWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set up Rewired" Height="520" Width="640" MinHeight="480" MinWidth="560"
        WindowStartupLocation="CenterOwner" ResizeMode="CanMinimize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Set up Rewired" FontSize="22" FontWeight="Bold" Foreground="{StaticResource SteamAccent}" />
            <TextBlock Style="{StaticResource RewiredSubtitle}"
                       Text="Install unlock, in-Steam UI, and plugin — then launch Steam from this app."
                       Margin="0,6,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Steam folder" Foreground="{StaticResource SteamTextSecondary}" Margin="0,0,0,4" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SteamPathBox" />
                <Button Grid.Column="1" Content="Detect" Margin="8,0,0,0" Click="DetectSteam_Click" />
            </Grid>
            <TextBlock x:Name="ReadinessText" TextWrapping="Wrap" Foreground="{StaticResource SteamTextMuted}" Margin="0,10,0,0" />

            <CheckBox x:Name="InstallUiCheck" Content="Install in-Steam UI (Millennium + Rewired plugin)" Margin="0,14,0,0" IsChecked="True" />
            <CheckBox x:Name="InstallOstCheck" Content="Install OpenSteamTool (unlock backend)" Margin="0,6,0,0" IsChecked="True" />
            <CheckBox x:Name="ShortcutCheck" Content="Create Desktop shortcut" Margin="0,6,0,0" IsChecked="True" />
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource RewiredPanel}">
            <ScrollViewer x:Name="LogScroll" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="LogText" TextWrapping="Wrap" Foreground="{StaticResource SteamTextSecondary}"
                           FontFamily="Consolas" FontSize="12" />
            </ScrollViewer>
        </Border>

        <WrapPanel Grid.Row="3" Margin="0,14,0,0" HorizontalAlignment="Right">
            <Button x:Name="LaunchSteamButton" Style="{StaticResource RewiredPrimaryButton}"
                    Content="Launch Steam" Margin="0,0,8,0" Visibility="Collapsed" Click="LaunchSteam_Click" />
            <Button Content="Close" Margin="0,0,8,0" Click="Close_Click" />
            <Button x:Name="RunButton" Style="{StaticResource RewiredPrimaryButton}"
                    Content="Install Rewired" Click="RunSetup_Click" />
        </WrapPanel>
    </Grid>
</Window>
